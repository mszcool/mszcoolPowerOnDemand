using System;

/// <summary>
/// Should support the following actions:
/// - start Azure VM via Azure APIs
/// - stop Azure VM via Azure APIs
/// - reboot Azure VM via Azure APIs
/// - get status details via management APIs or in-VM Metadata service
/// </summary>
class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello World!");
    }
}